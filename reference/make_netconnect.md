# Creates network connectivity info displayed under network plots

Creates network connectivity info displayed under network plots

## Usage

``` r
make_netconnect(freq)
```

## Arguments

- freq:

  List of NMA results created by freq_wrap().

## Value

Vector summarising network connectivity created by
netmeta::netconnection().
