# Calculate a sensible step value to use in numericInput

Calculate a sensible step value to use in numericInput

## Usage

``` r
format_step(x)
```

## Arguments

- x:

  numeric. The value to create a step for
