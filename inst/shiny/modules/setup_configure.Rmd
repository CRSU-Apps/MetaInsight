```{asis, echo = {{setup_configure_knit}}, eval = {{setup_configure_knit}}, include = {{setup_configure_knit}}}
### Configure the analysis and process the data into suitable formats for analysis
```

```{r, echo = {{setup_configure_knit}}, include = {{setup_configure_knit}}}
uncleaned_reference_treatment <- {{setup_configure_reference_treatment}}
ranking_option <- {{setup_configure_ranking_option}}
outcome_measure <- {{setup_configure_outcome_measure}}

configured_data <- setup_configure(uploaded_data, treatments, outcome, outcome_measure, uncleaned_reference_treatment)

wrangled_data <- configured_data$wrangled_data
treatment_df <- configured_data$treatment_df
disconnected_indices <- configured_data$disconnected_indices
main_connected_data <- configured_data$main_connected_data
non_covariate_data_all <- configured_data$non_covariate_data_all
bugsnet_all <- configured_data$bugsnet_all
frequentist_all <- configured_data$freq_all
reference_treatment_all <- configured_data$reference_treatment
```
