```{asis, echo = {{setup_define_knit}}, eval = {{setup_define_knit}}, include = {{setup_define_knit}}}
Define the type of the uploaded data and process it into suitable formats for analysis
```

```{r, echo = {{setup_define_knit}}, include = {{setup_define_knit}}}
reference_treatment <- {{setup_define_reference_treatment}}
ranking_option <- {{setup_define_ranking_option}}
outcome_measure <- {{setup_define_outcome_measure}}

defined_data <- setup_define(uploaded_data, treatments, outcome, outcome_measure, reference_treatment)

wrangled_data <- defined_data$wrangled_data
treatment_df <- defined_data$treatment_df
disconnected_indices <- defined_data$disconnected_indices
main_connected_data <- defined_data$main_connected_data
non_covariate_data_all <- defined_data$non_covariate_data_all
bugsnet_all <- defined_data$bugsnet_all
frequentist_all <- defined_data$freq_all
```
