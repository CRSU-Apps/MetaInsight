```{asis, echo = {{covariate_ranking_knit}}, eval = {{covariate_ranking_knit}}, include = {{covariate_ranking_knit}}}
### Generate ranking plots
```

```{r, echo = {{covariate_ranking_knit}}, include = {{covariate_ranking_knit}}}
covariate_ranking <- covariate_ranking(covariate_model, configured_data)
```

```{asis, echo = {{covariate_ranking_knit}}, eval = {{covariate_ranking_knit}}, include = {{covariate_ranking_knit}}}
{r, results = 'asis'}
```
```{r, echo = {{covariate_ranking_knit}}, include = {{covariate_ranking_knit}}}
covariate_forest(covariate_model, ranking = TRUE)
```

```{asis, echo = {{covariate_ranking_knit}}, eval = {{covariate_ranking_knit}}, include = {{covariate_ranking_knit}}}
{r, results = 'asis'}
```
```{r, echo = {{covariate_ranking_knit}}, include = {{covariate_ranking_knit}}}
if ({{covariate_ranking_rank_style}} == "radial"){
  RadialSUCRA(covariate_ranking, !{{covariate_ranking_simple}}, {{covariate_ranking_colourblind}})
} else {
  LitmusRankOGram(covariate_ranking, {{covariate_ranking_colourblind}})
}
```

```{r, echo = {{covariate_ranking_knit}}, include = {{covariate_ranking_knit}}}
knitr::kable(ranking_table(covariate_ranking), digits = 2)
```

```{asis, echo = {{covariate_ranking_knit}}, eval = {{covariate_ranking_knit}}, include = {{covariate_ranking_knit}}}
{r, results = 'asis'}
```
```{r, echo = {{covariate_ranking_knit}}, include = {{covariate_ranking_knit}}}
summary_network(configured_data, {{covariate_ranking_network_style}})
```
