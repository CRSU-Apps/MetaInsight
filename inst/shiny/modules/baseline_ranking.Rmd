```{asis, echo = {{baseline_ranking_knit}}, eval = {{baseline_ranking_knit}}, include = {{baseline_ranking_knit}}}
### Generate ranking plots
```

```{r, echo = {{baseline_ranking_knit}}, include = {{baseline_ranking_knit}}}
baseline_ranking <- baseline_ranking(baseline_model, configured_data)
```

```{asis, echo = {{baseline_ranking_knit}}, eval = {{baseline_ranking_knit}}, include = {{baseline_ranking_knit}}}
{r, results = 'asis'}
```
```{r, echo = {{baseline_ranking_knit}}, include = {{baseline_ranking_knit}}}
baseline_forest(baseline_model, ranking = TRUE)
```

```{asis, echo = {{baseline_ranking_knit}}, eval = {{baseline_ranking_knit}}, include = {{baseline_ranking_knit}}}
{r, results = 'asis'}
```
```{r, echo = {{baseline_ranking_knit}}, include = {{baseline_ranking_knit}}}
if ({{baseline_ranking_rank_style}} == "radial"){
  RadialSUCRA(baseline_ranking, !{{baseline_ranking_simple}}, {{baseline_ranking_colourblind}})
} else {
  LitmusRankOGram(baseline_ranking, {{baseline_ranking_colourblind}})
}
```

```{r, echo = {{baseline_ranking_knit}}, include = {{baseline_ranking_knit}}}
knitr::kable(ranking_table(baseline_ranking), digits = 2)
```

```{asis, echo = {{baseline_ranking_knit}}, eval = {{baseline_ranking_knit}}, include = {{baseline_ranking_knit}}}
{r, results = 'asis'}
```
```{r, echo = {{baseline_ranking_knit}}, include = {{baseline_ranking_knit}}}
summary_network(configured_data, {{baseline_ranking_network_style}})
```
